/** DS-14 — the one pill vocabulary and the Area identity mark. */

/** M3-INT — absence is supporting text, not a chip. See `./Absence`. */
export { AbsenceText, type AbsenceTextProps } from "./Absence";

export {
  AreaDot,
  AreaPill,
  AREA_ACCENT_COUNT,
  areaAccentForRank,
  StatusPill,
  type AreaDotProps,
  type AreaPillProps,
  type PillTone,
  type StatusPillProps,
} from "./Pill";
