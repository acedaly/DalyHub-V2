/**
 * PX-02 — public entry for the shared Skeleton system (DESIGN_SYSTEM.md → Loading).
 */

export {
  Skeleton,
  CardSkeleton,
  CollectionSkeleton,
  PaneSkeleton,
  type SkeletonProps,
  type CardSkeletonProps,
  type CollectionSkeletonProps,
} from "./Skeleton";
