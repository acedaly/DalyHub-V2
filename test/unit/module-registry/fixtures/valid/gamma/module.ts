/**
 * Test fixture manifest — NOT a product module. A third manifest added purely by
 * dropping this file in place: no central list was edited to make it discoverable.
 */
import { defineModule } from "~/kernel/modules";

export default defineModule({ id: "gamma", name: "Gamma" });
